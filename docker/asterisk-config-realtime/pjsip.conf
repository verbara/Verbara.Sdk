; PJSIP config for realtime server
; ALL endpoints, trunks, auth, and aors come from the database via sorcery.conf
; Only transport, system, and global stay in this file.

[system]
type = system

[global]
type = global

[transport-udp]
type = transport
protocol = udp
bind = 0.0.0.0:5060

[transport-wss]
type = transport
protocol = wss
bind = 0.0.0.0:8089

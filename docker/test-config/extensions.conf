[general]
static = yes
writeprotect = no

[default]
; Extension 100: Answer, wait, hangup (basic test)
exten => 100,1,Answer()
same => n,Wait(2)
same => n,Hangup()

; Extension 200: FastAGI test
exten => 200,1,Answer()
same => n,AGI(agi://tests:4573/test-script)
same => n,Hangup()

; Extension 300: Stasis ARI test
exten => 300,1,Answer()
same => n,Stasis(test-app)
same => n,Hangup()

; Extension 999: Echo test
exten => 999,1,Answer()
same => n,Echo()
same => n,Hangup()

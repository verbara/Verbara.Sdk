; PJSIP Transport — must stay file-based (realtime transports cause race conditions on startup)
; All endpoints, auth, and aors are loaded from database via sorcery.conf

[transport-udp]
type = transport
protocol = udp
bind = 0.0.0.0:5060

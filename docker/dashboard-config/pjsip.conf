; PJSIP file-based config — only transport, system, and global stay here.
; All endpoints, auth, and aors are loaded from database via sorcery.conf

[system]
type = system

[global]
type = global

[transport-udp]
type = transport
protocol = udp
bind = 0.0.0.0:5060

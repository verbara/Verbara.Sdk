# provider-schema-drift-train

Vendor the providers' own openly-licensed wire contracts, derive fixtures from them instead of from
prose, and put a weekly diff on the scheduled train so a vendor changing its protocol turns something
red

# a-published-surface-is-one-something-measures

Three surfaces this SDK publishes are connected to nothing that could tell whether they work: the
WebSocket audio server's lookup key, the ten audio metric instruments, and the functional tests that
dial extensions the dialplan never defines. Each is kept green by a test that cannot see the
difference. Measure first, then fix — and in the WebSocket case the measurement does not exist yet.

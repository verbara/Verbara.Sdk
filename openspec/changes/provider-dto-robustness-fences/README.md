# provider-dto-robustness-fences

Let a provider receive loop survive a frame it cannot parse, make the nullability annotations on
provider DTOs load-bearing on the read path, and fence the wire-drift class a recording only catches
on the day someone re-captures

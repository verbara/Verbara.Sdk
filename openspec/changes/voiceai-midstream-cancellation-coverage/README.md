# voiceai-midstream-cancellation-coverage

Cancel a WebSocket session with frames in flight — the test that gives `HangForever` a consumer, makes `HoldOpenUntilDisposed` falsifiable and witnesses `CloseSent`

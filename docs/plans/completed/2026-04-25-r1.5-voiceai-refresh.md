# R1.5 SDK v1.15.3 "VoiceAi Refresh" — Execution Plan

**Date:** 2026-04-25 (original) · **Revised:** 2026-05-03 (v2 — scope correction)
**Branch:** `r1.5-voiceai-refresh` (rebase onto current `main` before execution; current branch is 8 days stale, 14 commits behind)
**Spec:** [`docs/specs/2026-04-25-r1.5-voiceai-refresh-design.md`](../../specs/2026-04-25-r1.5-voiceai-refresh-design.md) (revised v2 same date as this plan)
**Cadence:** ship-oportunista — lands when ready, not gated to R5 cadence.
**Estimated effort:** ~1 sem with subagent-driven parallelization. **No Phase 0 AOT spike** in v2 (Whisper.net deferred).

---

## Revision history

| Version | Date | Change |
|---|---|---|
| v1 | 2026-04-25 | Original — Phase 0 AOT spike (Whisper.net) blocking, then 3 tracks (Flash 2.5 + Aura 2 REST + Whisper local) + TTFA. ~1 sem + 1-2 days spike. |
| v2 | 2026-05-03 | **Restructured** per spec v2: dropped Phase 0 (no native deps in v2 scope); replaced Whisper local track with **LMNT TTS new provider**; Aura 2 track upgraded from REST to **WebSocket** endpoint. Same total ~1 sem, lower risk, more product value. |

---

## Execution status (live)

| Phase | Status | Notes |
|---|---|---|
| **Pre-flight rebase** | ✅ Done 2026-05-03 | `r1.5-voiceai-refresh` rebased onto `main @ b49fed5`; force-push to origin completed; superseded v1 commit `565a1bb` dropped cleanly. |
| **Q1 probe** (Aura 2 WS protocol) | ✅ Done 2026-05-03 | Documented from public docs (no live API call — no `DEEPGRAM_API_KEY` available). Sources: developers.deepgram.com/reference/text-to-speech/speak-streaming. Contract handed to Track 1.B subagent. |
| **Q2 verification** (LMNT API contract) | ✅ Done 2026-05-03 | Documented from public docs (no live API call — no `LMNT_API_KEY` available). Sources: docs.lmnt.com/api/overview, /api/speech-sessions/create, /api/voices/list. Contract handed to Track 1.C subagent. |
| **Track 1.A** ElevenLabs Flash 2.5 | ✅ Done 2026-05-03 | +9 unit tests (16 ElevenLabs total). 0 build/AOT warnings. Default `ModelId` flips `eleven_turbo_v2` → `eleven_flash_v2_5`. |
| **Track 1.B** Deepgram Aura 2 WS | ✅ Done 2026-05-03 | +12 unit tests. New provider folder `Deepgram/`. 12-voice catalog (8 EN + 1 ES Sirio + 3 legacy Aura 1). TODO comment for unverified multilingual voice ids (NL/FR/DE/IT/JA). |
| **Track 1.C** LMNT TTS | ✅ Done 2026-05-03 | LMNT WebSocket + HTTP fallback. Test count delta brings TTS suite to 61 total (was 28 pre-Phase 1 → +33 net new across all 3 tracks). |
| **Phase 1 verification** | ✅ Done 2026-05-03 | `dotnet build -c Release`: 0/0 warn/err. TTS suite: 61 pass / 0 fail / 0 skip. `tools/verify-aot.sh`: 0 trim warnings. `dotnet pack` (TWAE): 26 packages clean. |
| **Phase 2** TTFA observability | ✅ Done 2026-05-03 | `tts.synthesis.ttfa_ms` histogram added to `SpeechSynthesisMetrics`; recorded in `VoiceAiPipeline` on first audio chunk yielded (NOT in base class — kept the existing pattern, zero breaking change). +5 unit tests in `VoiceAiPipelineTtfaTests`. Pipeline test suite 32 → 37. README updated with metric catalog + 6-provider table. **Deviation from spec:** spec mandated base-class wrapping, but that would have been a breaking change to the abstract `SynthesizeAsync` method; pipeline-side recording is non-breaking + consistent with existing `SynthesisLatencyMs` pattern. T2.3 (per-provider buffering assertion) and T2.6 (BenchmarkDotNet TTFA smoke) deferred to Phase 3 polish. |
| **Phase 3** Release engineering | ✅ Done 2026-05-03 | CHANGELOG + version bump 1.15.2 → 1.15.3 + PR #41 (8/8 CI checks green incl. new `dependency-review` workflow after enabling repo Dependency graph) + squash-merge to `main` as `2c03faf` + tag `v1.15.3` + `publish.yml` 26 packages → nuget.org (1m58s) + [GitHub Release v1.15.3](https://github.com/Harol-Reina/Asterisk.Sdk/releases/tag/v1.15.3) published with full CHANGELOG body. **Phase 1 → ship: same calendar day, ~4h wall-clock.** T2.3 deferred to follow-up patch (TTFA metric works correctly today; T2.3 is a regression-prevention gate). |

---

## Phases

### Phase 1 — Provider implementations (~4-5 días, parallel)

Three independent tracks, parallelizable with subagent-driven batching. Tracks are fully isolated from each other (different folders, different test files), so subagents do not collide.

#### Track 1.A — ElevenLabs Flash 2.5 extension (1 día, S)

Foreground or background subagent OK; small surface, low risk.

- T1.A.1 — Add [`ElevenLabsModels`](../../../src/Asterisk.Sdk.VoiceAi.Tts/ElevenLabs/) constants class (`Flash25 = "eleven_flash_v2_5"`, `Turbo2`, `Multilingual2`). Verify model id at impl time against [ElevenLabs models docs](https://elevenlabs.io/docs/overview/models).
- T1.A.2 — Extend `ElevenLabsOptions` with additive properties: `LatencyOptimization` enum (Off…Max, 0-4 scale per provider's `optimize_streaming_latency`), `OutputFormat` enum (Pcm16k / Pcm22050 / Pcm24k), `Model` default flip to `Flash25`.
- T1.A.3 — Update `ElevenLabsSpeechSynthesizer` WS connection to surface model + format + latency-optim parameters per provider docs.
- T1.A.4 — Unit tests: options binding, WS request shape verification (using mock `IClientWebSocket`), Flash 2.5 default behavior.
- T1.A.5 — Integration test (gated `RUN_LIVE_TESTS=true` + `ELEVENLABS_API_KEY` env): assert TTFA < 200 ms ceiling.
- T1.A.6 — `PublicAPI.Unshipped.txt` updated with new types.

#### Track 1.B — Deepgram Aura 2 TTS (new provider, **WebSocket** endpoint) (2 días, M)

Background subagent recommended. Mirror existing Deepgram STT structure for shape consistency.

- T1.B.0 — **Spec Q1 probe** (~½ día) — write a 50-line probe that connects to `wss://api.deepgram.com/v1/speak?model=aura-2-thalia-en`, sends one phrase via JSON `{"type":"Speak","text":"hello"}`, prints frame metadata (sizes, types, content-types, status events). Goal: verify the documented frame protocol matches reality. Probe lives in `Examples/` or as a one-off test fixture; deleted after T1.B.6.
- T1.B.1 — Create folder `src/Asterisk.Sdk.VoiceAi.Tts/Deepgram/` with options + synthesizer + DI extension + JSON serialization context entries.
- T1.B.2 — Implement `DeepgramSpeechSynthesizer : SpeechSynthesizer` with the **WebSocket** endpoint (`wss://api.deepgram.com/v1/speak?...`); auth `Authorization: Token <key>`; streaming PCM 16-bit / mu-law / a-law per `DeepgramTtsOptions.Encoding`. **Do NOT implement REST `/v1/speak` fallback in v1.15.3** — WS only.
- T1.B.3 — Add voice catalog constants `DeepgramVoices.Thalia`, `Andromeda`, etc. (per Aura 2 voice list, including 2026 multilingual additions).
- T1.B.4 — Health check: pings the Deepgram TTS lightweight endpoint on startup; degrades if 401/503.
- T1.B.5 — Unit tests: 8-10 covering options binding, WS request shape, frame yield correctness, error handling on disconnect.
- T1.B.6 — Integration test (gated, `DEEPGRAM_API_KEY` env): asserts TTFA < 250 ms ceiling + sample ≥ 1 second of audio frames yielded.
- T1.B.7 — `PublicAPI.Unshipped.txt` updated.

#### Track 1.C — LMNT TTS (new provider) (2 días, M)

Background subagent. Replaces Whisper V3 local track from v1. Mirrors existing TTS provider structure (e.g., Cartesia) for consistency.

- T1.C.0 — **Spec Q2 verification** (~½ día) — confirm LMNT API contract matches expectation against [LMNT API docs](https://docs.lmnt.com): exact auth header name, WS endpoint URL pattern, JSON message schema for token streaming, fallback HTTP endpoint shape. Document any deviations from spec assumptions in a comment block at the top of `LmntSpeechSynthesizer.cs`.
- T1.C.1 — Create folder `src/Asterisk.Sdk.VoiceAi.Tts/Lmnt/` with options + synthesizer + DI extension + JSON serialization context entries.
- T1.C.2 — Implement `LmntSpeechSynthesizer : SpeechSynthesizer` with WebSocket streaming preferred + HTTP streaming fallback (configurable via `LmntTtsOptions.Transport`). Auth header per LMNT docs.
- T1.C.3 — Add voice catalog `LmntVoices.<curated>` constants — limited to telephony-friendly low-latency voices (verify list at impl time).
- T1.C.4 — Health check: pings LMNT health endpoint on startup; degrades if 401/503.
- T1.C.5 — Unit tests: 6-8 covering options binding, WS / HTTP transport selection, frame yield correctness, fallback path.
- T1.C.6 — Integration test (gated, `LMNT_API_KEY` env): asserts TTFA < 250 ms ceiling.
- T1.C.7 — `PublicAPI.Unshipped.txt` updated.

---

### Phase 2 — TTFA observability + cross-cutting (1 día)

Foreground (depends on Phase 1 completion — TTFA recording is base-class behavior, applies to all providers including the new ones).

- T2.1 — Add `tts.synthesis.ttfa_ms` `Histogram<double>` to [`SpeechSynthesisMetrics`](../../../src/Asterisk.Sdk.VoiceAi/Diagnostics/SpeechSynthesisMetrics.cs) with description "Time-to-first-audio: elapsed from synthesis start until first audio frame yielded to caller." Tags: `voiceai.provider`, `tts.model`.
- T2.2 — Modify `SpeechSynthesizer` base class to wrap `SynthesizeAsync` and record TTFA on first yielded byte. Use a `Stopwatch` started at method entry; record once on first frame.
- T2.3 — Verify all 6 TTS providers (Azure, Cartesia, ElevenLabs, Speechmatics + new Deepgram, LMNT) yield first frame promptly (no buffering pre-yield). Add a unit test per provider asserting `first_byte_offset < total_synthesis_duration` (i.e., the synthesizer is genuinely streaming, not collecting). For any provider that fails: file an issue, fix or document the buffer-then-yield pattern.
- T2.4 — Update [`src/Asterisk.Sdk.VoiceAi.Tts/README.md`](../../../src/Asterisk.Sdk.VoiceAi.Tts/README.md) listing the new histogram with default histogram buckets per Asterisk OTel conventions (likely 5/10/25/50/100/250/500/1000/2500/5000 ms).
- T2.5 — Unit tests: 4-5 covering TTFA recording correctness (records on first yield, not on subsequent yields, not on completion).
- T2.6 — Bench test: BenchmarkDotNet smoke in `Tests/Asterisk.Sdk.Benchmarks/` exercising TTFA path (env-gated, IT-style).

---

### Phase 3 — Release engineering (~0.5 día)

- T3.1 — `CHANGELOG.md` entry `[1.15.3]` with: (a) ElevenLabs Flash 2.5 model surface + LatencyOptimization + OutputFormat; (b) Deepgram Aura 2 WebSocket TTS new provider; (c) LMNT TTS new provider; (d) `tts.synthesis.ttfa_ms` histogram added; (e) breaking-change call-out: ElevenLabs default model flips from `eleven_turbo_v2` → `eleven_flash_v2_5` (non-breaking-API, default-change documented). Note: **Whisper V3 local NOT shipped in v1.15.3** (deferred to future on-prem privacy track) — surface this clearly to set expectations for anyone tracking the v1 spec.
- T3.2 — `Directory.Build.props` bump version `1.15.2` → `1.15.3`.
- T3.3 — Run full test suite locally: `dotnet test Asterisk.Sdk.slnx --filter "Category!=Functional&Category!=Integration"` — expect zero new failures, +30-40 net new tests.
- T3.4 — AOT publish smoke: `bash tools/verify-aot.sh` (the same script the `aot-check` CI job invokes). Zero IL3050/IL3053 expected. **No new suppressions** expected — v2 scope adds zero native interop.
- T3.5 — Pack: `dotnet pack -c Release -o /media/Data/Source/IPcom/local-nuget-feed/` for the touched packages: `Asterisk.Sdk.VoiceAi`, `Asterisk.Sdk.VoiceAi.Tts`. (Stt unchanged — Whisper local removed from scope.)
- T3.6 — Final commit on branch with version bump + changelog. Merge `r1.5-voiceai-refresh` → `main` via PR (or fast-forward if branch is fully clean against main).
- T3.7 — Tag `v1.15.3` + GH Release with marketing blurb naming Flash 2.5 / Aura 2 WS / LMNT.

---

## Subagent-Driven execution rules

- **Q1 probe (T1.B.0) and Q2 verification (T1.C.0):** foreground, sequenced before each track's implementation. Cheap (~½ day each); answer is required before committing 1-2 days of impl per track.
- **Phase 1 Tracks A + B + C (background, parallel):** launch 3 subagents in single message after Q1/Q2 resolved, monitor with completion notifications. Each track is fully isolated — different folders, different tests, no shared state.
- **Phase 2 (foreground):** depends on Phase 1; sequential after parallel tracks merge.
- **Phase 3 (foreground):** release engineering requires user input on tag/release timing.

**Per-subagent prompt template:**
- "You are implementing R1.5 Track 1.X. Spec: `[path]`. Existing reference impl: `[path to most-similar provider — Cartesia for LMNT, Deepgram STT for Aura 2 TTS, ElevenLabs current for Flash 2.5]`. Strict requirements: zero AOT warnings, AOT-safe DI (no reflection), JSON serializer context registered, health check auto-registered, unit tests covering happy path + 2 edge cases per public method, `PublicAPI.Unshipped.txt` updated."
- Each subagent reports: files created/modified, test count delta, AOT publish output, any deviations from spec with rationale.

---

## Verification per phase

| Phase | Verification step | Pass criteria |
|---|---|---|
| Phase 1.A | `dotnet test Tests/Asterisk.Sdk.VoiceAi.Tts.Tests/ --filter "FullyQualifiedName~ElevenLabs"` | All pass; +5-7 tests |
| Phase 1.B | `dotnet test Tests/Asterisk.Sdk.VoiceAi.Tts.Tests/ --filter "FullyQualifiedName~Deepgram"` | All pass; +8-10 tests; Q1 probe deleted |
| Phase 1.C | `dotnet test Tests/Asterisk.Sdk.VoiceAi.Tts.Tests/ --filter "FullyQualifiedName~Lmnt"` | All pass; +6-8 tests; Q2 verification documented in source |
| Phase 2 | `tts.synthesis.ttfa_ms` histogram exported by all 6 TTS providers (Azure, Cartesia, ElevenLabs, Speechmatics, Deepgram, LMNT) | All yield TTFA < 500 ms in unit tests; per-provider streaming-not-buffering assertion passes |
| Phase 3 | `dotnet pack -c Release` produces NuGet packages with version 1.15.3 + zero warnings | CI green, local feed updated, AOT smoke clean |

---

## Risks / open items

- **Branch lifecycle:** the existing `r1.5-voiceai-refresh` branch is 8 days stale and 14 commits behind `main`. **Rebase first** (`git checkout r1.5-voiceai-refresh && git rebase main`) before adding any commits — the recent main has coverlet 10.0.0 + dependency-review workflow + xunit research that must travel with the R1.5 work.
- **GH Actions CI:** verify the `nuget.config` override pattern still works for v1.15.3 — same pattern as v1.15.0/.1/.2.
- **Pro consumer wiring (Track 3 in spec):** out of R1.5 scope. Coordinate with Pro v1.13.x-pro patch separately when scheduled. **Pro is NOT waiting for v1.15.3** — Pro 1.16.0-pro shipped 2026-04-28 consuming SDK 1.15.1; this train is independent.
- **Whisper V3 local removed from v1.15.3:** anyone watching the v1 spec for Whisper local should be redirected to the future on-prem privacy track. Add a `CHANGELOG.md` note + a `docs/research/` follow-up explaining the deferral rationale (telephony 8 kHz quality concern + Whisper.net AOT unconfirmed) so the deferral is transparent. *(Optional but recommended: open a follow-up issue / ADR for "on-prem privacy STT track" so the Whisper deferral has a concrete next-step pointer.)*
- **Backward compat:** ElevenLabs default model flips from `eleven_turbo_v2` → `eleven_flash_v2_5`. Document in CHANGELOG as a "non-breaking default change" (callers who explicitly set `Model` see no change; callers using default see the new model). If concern surfaces during review, add `[Obsolete]` warning hint or keep Turbo2 default with a `Recommended` flag.
- **LMNT live tests rate-limited on free tier:** mark IT with `Trait("Provider", "LMNT")` and document in CONTRIBUTING that live tests require a paid key (or skip).
- **Deepgram WebSocket frame protocol:** Q1 probe must complete cleanly before T1.B.1 starts. If the probe reveals a substantial divergence from documented schema, scope-update before committing more time.

---

## Cross-references

- Spec (revised v2): [`docs/specs/2026-04-25-r1.5-voiceai-refresh-design.md`](../../specs/2026-04-25-r1.5-voiceai-refresh-design.md)
- Coverlet bump + xunit research (recent on `main`): [`docs/plans/active/2026-05-03-coverlet-v10-bump-and-xunit-tracking.md`](2026-05-03-coverlet-v10-bump-and-xunit-tracking.md)
- Release-train envelope: `Asterisk.Platform/docs/plans/active/2026-04-22-r5-production-readiness-release-train.md` §325-340
- Triage trigger: `Asterisk.Platform/docs/plans/active/2026-04-25-r5.1-post-ship-triage.md` D-FORCE-4
- Pro AgentAssistBuilder reference: `Asterisk.Sdk.Pro/src/Asterisk.Sdk.Pro.AgentAssist/DependencyInjection/AgentAssistBuilder.cs`
- Original v1 plan: see git history at commit `565a1bb` (file path unchanged).

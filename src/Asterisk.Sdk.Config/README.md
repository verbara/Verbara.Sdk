# Asterisk.Sdk.Config

Parsers for Asterisk `.conf` configuration files. Reads and interprets Asterisk configuration format including contexts, variables, and includes.

## Features

- `ConfigFileReader` — general-purpose `.conf` file parser
- `ExtensionsConfigFileReader` — specialized parser for `extensions.conf`
- Native AOT compatible

<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <Description>AudioSocket transport for Asterisk.Sdk — bidirectional PCM audio streaming over TCP using the Asterisk AudioSocket protocol.</Description>
    <PackageTags>$(PackageTags);audiosocket;voiceai;audio;streaming</PackageTags>
  </PropertyGroup>
  <ItemGroup>
    <InternalsVisibleTo Include="Asterisk.Sdk.VoiceAi.AudioSocket.Tests" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\Asterisk.Sdk.Audio\Asterisk.Sdk.Audio.csproj" />
  </ItemGroup>
  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" />
    <PackageReference Include="Microsoft.Extensions.Hosting.Abstractions" />
    <!-- System.IO.Pipelines is part of net10.0 shared framework -->
  </ItemGroup>
</Project>
